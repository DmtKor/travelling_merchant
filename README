This program demonstrates two methods used to solve travelling merchant problem and basic usage of pygame. It was not written to show high performance or most efficient methods. Since there's no venv, you need to have pygame installed (and Python itself) to run this code.
